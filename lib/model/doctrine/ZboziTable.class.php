<?php

/**
 * ZboziTable
 * 
 * This class has been auto-generated by the Doctrine ORM Framework
 */
class ZboziTable extends Doctrine_Table
{
    /**
     * Returns an instance of this class.
     *
     * @return object ZboziTable
     */
    public static function getInstance()
    {
        return Doctrine_Core::getTable('Zbozi');
    }
    
function retrieveForSelect($q, $limit = 5) {
        $q = Doctrine_Query::create()
                        ->from('Zbozi')
                        ->andWhere('name like ?', '%' . $q . '%')
                        ->addOrderBy('name')
                        ->limit($limit);
        $items = array();
        foreach ($q->execute() as $item) {
            $items[$item->getId()] = (string) $item;
        }
        return $items;
    }    
}